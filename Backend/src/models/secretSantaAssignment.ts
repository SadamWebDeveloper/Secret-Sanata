
// typescript we need to define its strcture of type
export interface Employee {
  Employee_Name: string;
  Employee_EmailID: string;
}

export interface Assignment {
  Employee_Name: string;
  Employee_EmailID: string;
  Secret_Child_Name: string;
  Secret_Child_EmailID: string;
}
